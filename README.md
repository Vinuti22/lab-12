📋 Отчет о проделанной работе: Система симуляции аутентификации на JWT💡 Обзор проектаДанный проект представляет собой простую имитацию механизма аутентификации пользователя на фронтенде с использованием JSON Web Tokens (JWT). Все компоненты реализованы на чистом ванильном JavaScript и хранят "токен" в localStorage браузера.Цель: Продемонстрировать логику входа, сохранения, использования и удаления токена без необходимости разворачивания реального бэкенда.✨ Основные добавления и функциональностьВ приложении реализованы следующие ключевые функции и компоненты:1. 🔑 Логика аутентификации (JS)ФункцияОписаниеgenerateFakeJwt(email)Генерация "фейкового" токена. При успешной отправке формы создает строку в формате header.payload.signature, имитирующую настоящий JWT. Токен кодирует предоставленный email.login(token)Сохранение токена. Получает сгенерированный токен и сохраняет его в браузере под ключом TOKEN_KEY = 'authToken' с помощью localStorage.setItem().logout()Удаление токена. Удаляет ключ authToken из localStorage с помощью localStorage.removeItem().2. 🖥️ Управление интерфейсом и состояниемФункцияОписаниеrenderApp()Основная функция рендеринга. Проверяет наличие токена в localStorage.Если токен найден, отображается защищенный контент (#app-content) и скрывается форма входа.Если токен отсутствует, отображается форма входа (#login-form).3. 🔄 Механизм автоматического входаДобавлен обработчик события document.addEventListener('DOMContentLoaded', renderApp).Это гарантирует, что при каждой перезагрузке страницы функция renderApp проверяет localStorage.Если действующий токен существует, пользователь автоматически видит контент приложения, минуя форму входа.4. 🖼️ Компоненты UI (HTML)Форма входа (#login-form): Принимает E-mail и Пароль. При отправке формы (даже с фейковыми данными) запускается процесс генерации токена.Защищенный контент (#app-content): Отображается только после входа. Содержит:Поле для отображения сохраненного JWT-токена.Кнопку "Выход" (#logout-button), которая вызывает функцию logout().🛠️ Как запустить проектСохраните код в файл с именем index.html.Откройте файл index.html в любом современном браузере.Откройте Инструменты разработчика (F12) и перейдите на вкладку Application -> Storage -> Local Storage, чтобы видеть, как сохраняется и удаляется токен.⚙️ ТехнологииHTML5CSS3 (минимум для стилизации)Vanilla JavaScript (ES6+)localStorage для хранения состояния аутентификации.btoa() для имитации кодирования JWT (Base64).
