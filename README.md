<img width="450" height="288" alt="image" src="https://github.com/user-attachments/assets/a19e3c24-5e49-46d1-a7b9-d2efe96e8eb9" />
<img width="441" height="319" alt="image" src="https://github.com/user-attachments/assets/39c6ffc3-1112-4e36-b398-aed56e322be3" />

📋 Отчёт о проделанной работе: Система симуляции аутентификации на JWT

💡 Краткий обзор проекта

Проект представляет собой простую фронтенд-симуляцию механизма аутентификации пользователей с использованием JSON Web Tokens (JWT). Вся логика реализована на чистом JavaScript, а "токен" хранится в localStorage браузера.

Цель проекта: показать работу базовой логики входа, сохранения, проверки и удаления токена без необходимости разворачивать настоящий сервер.

✨ Реализованная функциональность
1. 🔑 Логика аутентификации (JavaScript)
Функция	Описание
generateFakeJwt(email)	Создаёт псевдо-JWT в формате header.payload.signature. Email кодируется в Base64 через btoa().
login(token)	Получает токен и сохраняет его в localStorage под ключом authToken.
logout()	Удаляет токен из localStorage с помощью localStorage.removeItem().
2. 🖥️ Работа интерфейса и состояния

Основной элемент — функция renderApp(), которая:

проверяет, сохранён ли токен в localStorage;

если токен есть — отображает защищённый контент (#app-content) и скрывает форму входа;

если токена нет — показывает форму входа (#login-form).

3. 🔄 Автоматическая авторизация

На событие DOMContentLoaded добавлен вызов renderApp, благодаря чему:

при обновлении страницы состояние аутентификации автоматически восстанавливается;

если токен сохранён — пользователь сразу попадает в защищённую часть приложения.

4. 🖼️ Компоненты интерфейса (HTML)

Форма входа (#login-form)
Принимает Email и Пароль (любые данные подходят). При отправке генерируется токен.

Защищённый контент (#app-content)
Показывается только после успешного "входа". Содержит:

отображение текущего JWT-токена;

кнопку «Выход» (#logout-button), вызывающую logout().

🛠️ Как запустить проект

Сохраните код в файл index.html.

Откройте этот файл в любом современном браузере.

Чтобы увидеть работу localStorage, откройте DevTools (F12) → вкладка Application → раздел Local Storage.

⚙️ Используемые технологии

HTML5

CSS3 (минимальная стилизация)

Vanilla JavaScript (ES6+)

localStorage — для хранения токена

btoa() — для имитации кодирования частей JWT в Base64
